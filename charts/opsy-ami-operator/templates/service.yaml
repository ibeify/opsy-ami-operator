apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsy-ami-operator.fullname" . }}
  labels:
    {{- include "opsy-ami-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opsy-ami-operator.selectorLabels" . | nindent 4 }}
